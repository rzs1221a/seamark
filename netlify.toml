[build]
  command = "npm run build"
  publish = "dist"

# Prerendered static HTML exists for every route; this only catches
# client-side navigations to unknown paths on a hard refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
